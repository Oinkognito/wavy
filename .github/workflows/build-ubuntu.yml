name: "Wavy CI / Ubuntu Build (x86-64 arch - GCC)"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false

    steps:
    - name: Checkout repository with submodules
      uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: Install dependencies for Wavy
      run: |
        sudo apt-get update
        xargs -a ci/apt-packages.txt sudo apt-get install -y

    - name: Build Release Version of Wavy (x86-64 arch) - GCC (with GNU-ld)
      run: |
        sudo make init
        make
